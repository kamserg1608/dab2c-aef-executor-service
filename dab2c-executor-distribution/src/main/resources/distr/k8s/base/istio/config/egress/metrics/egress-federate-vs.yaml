apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: egressgateway-federate-@fpi-name@-unver
spec:
  exportTo:
    - .
  hosts:
    - 'metrics.{{ lookup("custom_vars", "global.multiClusters.openshiftCluster") | regex_replace("^https?://", "") | regex_replace("^api\\.", "") | regex_replace(":[^/:]*", "") | regex_replace("/[^/]*$", "") }}'
  gateways:
    - mesh
    - egressgateway-federate-@fpi-name@-unver
  http:
    - match:
        - gateways:
            - mesh
          port: 80
      route:
        - destination:
            host: egressgateway-federate-@fpi-name@-unver
            port:
              number: 2001
    - match:
        - gateways:
            - egressgateway-federate-@fpi-name@-unver
          port: 2001
      route:
        - destination:
            host: 'metrics.{{ lookup("custom_vars", "global.multiClusters.openshiftCluster") | regex_replace("^https?://", "") | regex_replace("^api\\.", "") | regex_replace(":[^/:]*", "") | regex_replace("/[^/]*$", "") }}'
            port:
              number: 8481
