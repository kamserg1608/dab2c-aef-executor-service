apiVersion: v1
kind: Service
metadata:
  labels:
    app: router-executor-router-executor
  name: router-executor-router-executor-${distrib.release.version}
spec:
  ports:
    - name: http-8080
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    deploymentconfig: router-executor-router-executor-${distrib.release.version}
  type: ClusterIP
