dab2c:
  integration-service:
    session:
      da-cookie:
        session-ttl-seconds: 3
        path: "/testpath"
        domain: "test-domain"

grpc:
  server:
    port: 29090
  client:
    downstream:
      address: static://localhost:29091
      negotiation-type: plaintext

logging:
  level:
    root: DEBUG
    ru.sbrf.ufs.dab2c.core.voice: DEBUG
    net.devh.boot.grpc: DEBUG